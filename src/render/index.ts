/**
 * Rendering System
 * 
 * Handles all visual rendering:
 * - Camera: Viewport position, zoom, bounds
 * - Renderer: Canvas rendering, draw calls, layers
 */

export { Renderer } from './Renderer.js';
export type { RenderItem } from './Renderer.js';
export { Camera } from './Camera.js';
